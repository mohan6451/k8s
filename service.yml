apiVersion: v1
kind: Service
metadata:
  name: nginx
spec:
  selector:
    type: pod
    sector: practice
    project: awan
  ports:
    - protocal: TCP
      port: 80 # service port
      targetPort: 80 # container Port
